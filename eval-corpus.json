{
  "regression": [
    { "id": "trivial", "expected": "lightweights", "prompt": "what is 2+2?" },
    { "id": "quick-resume", "expected": "lightweights", "prompt": "summarize this in one line: the market is going up", "note": "capped by quick intent" },
    { "id": "lista-git", "expected": "lightweights", "prompt": "give me a quick list of git commands" },
    { "id": "explain-rl", "expected": "lightweights", "prompt": "explain how rate limiting works in a REST API" },
    { "id": "sql-index", "expected": "lightweights", "prompt": "write a SQL query with an index to search orders by customer and explain the query plan" },
    { "id": "ui-tweak", "expected": "lightweights", "prompt": "change the primary button color and align the dashboard header" },
    { "id": "code-fn", "expected": "lightweights+", "prompt": "implement a function that validates emails in TypeScript with tests:\n```ts\nfunction isValidEmail(s: string): boolean {}\n```\nfile: src/utils/email.ts" },
    { "id": "unit-test", "expected": "lightweights+", "prompt": "write a unit test for the discount function in src/utils/discount.ts and update the mocks" },
    { "id": "refactor-multi", "expected": "lightweights+", "prompt": "refactor the auth module. First read src/auth/*.ts, then separate the token logic into its own service and update the tests. Multiple files are involved." },
    { "id": "debug-diff", "expected": "lightweights+", "prompt": "debug this failure:\ndiff --git a/src/app.ts b/src/app.ts\n--- a/src/app.ts\n+++ b/src/app.ts\n@@ -10,7 +10,7 @@\n it gives me a 500 when calling /api/orders" },
    { "id": "refactor-ctx", "expected": "lightweights+", "prompt": "refactor the authentication handler to extract refresh token logic into a separate service. First read src/auth/handler.ts, src/auth/tokens.ts and src/auth/types.ts, propose the new design, and apply the changes with updated tests." },
    { "id": "micro-full", "expected": "workhorses", "prompt": "implement a complete payments microservice system: design the database schema, write REST endpoints with validation and error handling in Go, create integration tests with mocks, document the API with OpenAPI, and plan the rollout in phases. This is an end-to-end task. There are several files involved: internal/api/*.go, internal/domain/*.go, migrations/*.sql. Write a detailed report explaining the complete process." },
    { "id": "critical-prod", "expected": "sota", "prompt": "change the service port in production and deploy, with no backup", "note": "floor by criticality" },
    { "id": "arch-payments", "expected": "sota", "prompt": "design the complete payments system architecture: write a detailed report with the plan, database migration, security strategy, and formal proposal (RFC)", "note": "floor by criticality" }
  ],
  "aspirational": [
    {
      "id": "wh-endpoint",
      "ideal": "workhorses",
      "prompt": "Implement the webhook endpoint with exponential retries and HMAC signature. This is the current handler that returns 500:\n```ts\nasync function handle(req: Request) {\n  const body = await req.json();\n}\n```\nRefactor src/webhooks/handler.ts and src/lib/signature.ts, write integration tests and update OpenAPI.",
      "note": "medium code task with failing snippet -> under-scores to lightweights+ (current weights)"
    },
    {
      "id": "whplus-heavy",
      "ideal": "workhorses+",
      "contextPercent": 0.7,
      "prompt": "Refactor the event system: the Kafka consumer crashes in production due to poor error handling. Redesign processing with retries, dead-letter queue, and idempotence. First read src/events/consumer.ts and src/events/outbox.ts, then propose the new design, write tests, and document the complete process. Several modules involved.",
      "note": "heavy production refactoring -> under-scores to workhorses"
    },
    {
      "id": "sota-beast",
      "ideal": "sota+",
      "contextPercent": 0.85,
      "prompt": "Design and implement the new distributed payments system architecture for production: Kafka events with CQRS, database migration with no downtime, security and compliance plan, ADRs, and phased rollout with rollback. Coordinate infra modules in parallel and write the detailed formal proposal report. Current state of the consumer:\n```go\nfunc main() {\n    consumer, _ := kafka.NewConsumer()\n}\n```\n```sql\nCREATE TABLE payments (\n    id UUID PRIMARY KEY\n);\n```\ndiff --git a/internal/payments/service.go b/internal/payments/service.go\n--- a/internal/payments/service.go\n+++ b/internal/payments/service.go\n@@ -12,7 +12,7 @@\n write also the migration plan and security strategy for the production rollout",
      "note": "frontier-grade task -> under-scores to sota (the 0.82 sota+ threshold demands near-maximal signals)"
    }
  ]
}
